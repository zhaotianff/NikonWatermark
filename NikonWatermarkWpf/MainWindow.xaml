<Window x:Class="NikonWatermarkWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NikonWatermarkWpf"
        xmlns:vm="clr-namespace:NikonWatermarkWpf.ViewModels"
        mc:Ignorable="d"
        Title="Nikon Watermark Generator" Height="700" Width="1000"
        Background="#1E1E1E">
    
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <!-- Dark Theme Styles -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Margin" Value="5,3"/>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Nikon Photo Watermark Generator" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,15"
                   Foreground="#FFFFFF"/>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Import -->
            <Border Grid.Column="0" BorderBrush="#3F3F46" BorderThickness="1" 
                    Background="#252526" CornerRadius="5" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Content="Import Images" FontSize="14"/>
                    
                    <ListBox Grid.Row="1" Margin="0,5"
                             ItemsSource="{Binding ImportedImages}"
                             DisplayMemberPath="FileName"/>
                    
                    <Button Grid.Row="2" Content="Import Images" 
                            Command="{Binding ImportImagesCommand}"
                            HorizontalAlignment="Stretch" Margin="0"/>
                </Grid>
            </Border>

            <!-- Middle Panel - Settings -->
            <Border Grid.Column="1" Width="250" Margin="10,0"
                    BorderBrush="#3F3F46" BorderThickness="1" 
                    Background="#252526" CornerRadius="5" Padding="10">
                <StackPanel>
                    <Label Content="Settings" FontSize="14" Margin="0,0,0,10"/>
                    
                    <Label Content="EXIF Fields:" FontSize="12" Margin="0,5,0,5"/>
                    <CheckBox Content="Aperture (f-stop)" IsChecked="{Binding ShowAperture}"/>
                    <CheckBox Content="ISO" IsChecked="{Binding ShowIso}"/>
                    <CheckBox Content="Shutter Speed" IsChecked="{Binding ShowShutterSpeed}"/>
                    
                    <Label Content="Watermark Position:" FontSize="12" Margin="0,15,0,5"/>
                    <ComboBox ItemsSource="{Binding PositionOptions}"
                              SelectedIndex="{Binding SelectedPositionIndex}"
                              Margin="5"/>
                </StackPanel>
            </Border>

            <!-- Right Panel - Export -->
            <Border Grid.Column="2" BorderBrush="#3F3F46" BorderThickness="1" 
                    Background="#252526" CornerRadius="5" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Content="Export Results" FontSize="14"/>
                    
                    <ListBox Grid.Row="1" Margin="0,5"
                             ItemsSource="{Binding ExportedImages}"/>
                    
                    <Button Grid.Row="2" Content="Process Images" 
                            Command="{Binding ProcessImagesCommand}"
                            HorizontalAlignment="Stretch" Margin="0"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#007ACC" CornerRadius="3" 
                Padding="8,4" Margin="0,10,0,0">
            <TextBlock Text="战歌，启动！Ready to process images" 
                       Foreground="White" FontSize="12"/>
        </Border>
    </Grid>
</Window>
